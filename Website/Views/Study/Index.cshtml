@model List<Website.Models.Card>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

@foreach (var card in Model)
{
    @Html.Partial("_card", card)
}

<div>
    @Html.ActionLink("Edit", "Edit", new { /* id = Model.PrimaryKey */ }) |
    <a asp-action="Index">Back to List</a>
</div>
